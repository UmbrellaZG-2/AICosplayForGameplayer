# 测试环境配置
# 服务器端口
server.port=8081

# 数据库配置 - 使用生产环境MySQL数据库
spring.datasource.url=jdbc:mysql://101.200.43.186:3306/aicosplay?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=aicosplay
spring.datasource.password=a029b7d039
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 语音识别模式配置 - 仅使用离线识别
# 优先使用离线模式，禁用在线模式
speech.recognition.default-mode=offline-first

# 禁用在线语音识别相关配置
xfyun.appid=test-appid
xfyun.api-key=test-api-key
xfyun.api-secret=test-api-secret
xfyun.host-url=http://localhost:9999

# 测试环境中禁用OpenAI API调用
spring.ai.openai.api-key=test-key
spring.ai.openai.chat.model=test-model

# Vosk离线语音识别配置
vosk.model.path=../../vosk-model-small-cn-0.22/
vosk.sample.rate=16000

# 日志配置
logging.level.com.aicosplay=DEBUG
logging.level.root=INFO